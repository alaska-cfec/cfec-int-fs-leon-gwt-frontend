get:
  summary: Get forms for entity
  tags: [Forms]
  operationId: getForms
  parameters:
    - name: id
      in: query
      required: true
      schema:
        type: string
    - name: ryear
      in: query
      required: true
      schema:
        type: string
  responses:
    '200':
      description: List of forms
      content:
        application/json:
          schema:
            type: array
            items:
              type: string
