post:
  summary: Kill session
  tags: [Session]
  operationId: killSession
  responses:
    '200':
      description: Success string
      content:
        application/json:
          schema:
            type: string
